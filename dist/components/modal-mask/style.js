export const style = `
/*<component>modal-mask*/
:host {
  --bg: rgba(0,0,0,.5);
}
:host {
  box-sizing: border-box;
  position: fixed;
  overflow: hidden;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  background-color: var(--bg);
}
`;
