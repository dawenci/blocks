import { makeTemplate } from '../../common/template.js';
export const layoutTemplate = makeTemplate(`<div id="layout"></div>`);
export const loadingTemplate = makeTemplate(`<span part="loading" id="loading"></span>`);
export const prefixTemplate = makeTemplate(`<span part="prefix" id="prefix"></span>`);
export const suffixTemplate = makeTemplate(`<span part="suffix" id="suffix"></span>`);
